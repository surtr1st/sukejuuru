{
    "name": "server",
    "version": "1.0.50",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "bun run --watch src/index.ts",
        "format": "prettier --write src/"
    },
    "dependencies": {
        "elysia": "latest",
        "prettier": "3.3.3"
    },
    "devDependencies": {
        "bun-types": "latest"
    },
    "module": "src/index.js"
}
