{
    "name": "server",
    "version": "1.0.50",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "bun run --watch src/index.ts",
        "format": "prettier --write src/"
    },
    "dependencies": {
        "drizzle-orm": "^0.33.0",
        "elysia": "latest",
        "postgres": "^3.4.4",
        "prettier": "3.3.3"
    },
    "devDependencies": {
        "bun-types": "latest",
        "drizzle-kit": "^0.24.0"
    },
    "module": "src/index.js"
}
